spring:
  application:
    name: account-service
  cloud:
    config:
      enabled: true
      uri: https://ehroute-config-server.herokuapp.com
      fail-fast: false
      retry:
        max-attempts: 20
        max-interval: 15000
        initial-interval: 5000
