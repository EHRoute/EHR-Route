spring:
  profiles:
    active: native
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MuizMahdi/EHR-Route.git
          search-paths: server-configs/
        native:
          search-locations: classpath:server-configs/