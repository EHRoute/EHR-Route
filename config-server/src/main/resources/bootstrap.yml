spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/EHRoute/ServicesConfigs.git
          timeout: 4
          clone-on-start: true
          skip-ssl-validation: true
