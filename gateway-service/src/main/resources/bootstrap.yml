spring:
  application:
    name: gateway-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888
      fail-fast: false
      #retry:
        #max-attempts: 20
        #max-interval: 15000
        #initial-interval: 5000