spring:
  application:
    name: provider-service
  cloud:
    config:
      enabled: false
      uri: https://ehroute-config-server.herokuapp.com
      fail-fast: false
      retry:
        max-attempts: 20
        max-interval: 15000
        initial-interval: 5000
